{
  "name": "GIS 584 Python Dev",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.11"
    }
  },
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "python.terminal.activateEnvironment": true
      },
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libgdal-dev gdal-bin python3-gdal && curl -LsSf https://astral.sh/uv/install.sh | sh && python -m venv .venv && . .venv/bin/activate && pip install --upgrade pip && pip install -r requirements.txt",
  "remoteEnv": {
    "PIP_DISABLE_PIP_VERSION_CHECK": "1",
    "PATH": "/home/vscode/.local/bin:${containerEnv:PATH}"
  }
}

